{
  "private": true,
  "scripts": {
    "build-test-client": "esbuild output/Test.Client/index.js --bundle --outfile=test/client.js",
    "test": "spago build && npm run build-test-client && spago test"
  },
  "devDependencies": {
    "esbuild": "^0.11.20",
    "pulp": "^15.0.0",
    "puppeteer": "^9.1.1",
    "purescript-psa": "^0.8.2",
    "rimraf": "^3.0.2"
  }
}
