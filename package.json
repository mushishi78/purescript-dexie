{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --  --strict"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "puppeteer": "^9.1.1",
    "purescript-psa": "^0.8.2",
    "rimraf": "^3.0.2"
  }
}
